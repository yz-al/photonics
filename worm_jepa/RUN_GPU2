mechanistic-correction rerun 53d5d37: test "self-detectable SOTA errors are fixable by the mechanistic process" on a HEALTHY encoder. Fixes: _merge picks JEPA-dependent analysis (DBB + edge + correction) from healthiest seed (not seed 0); anti-collapse EMA=0.998 + VAR=0.6. Full dense(1,16)+sparse(500,5000) + SOTA error detector + error composition + mechanistic correction (sparse boundary model re-decides low-confidence edges) + double black box. 3 seeds parallel A10G. approved ~$4-6. 2026-08-02T21:55Z
