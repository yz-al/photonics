lean routing-bound run ebcf250: the cheap measurement that bounds the confidence-routing program (per the conditional-scoring reframe). Well-trained SOTA (dense-16), minimal JEPA (bound uses only SOTA confidence), no context heads / beat / dbb. Reports at 80% error recall: enrichment, flag fraction, and the merge/split composition of MISSED (confident) vs FLAGGED errors — if missed errors are merge-heavy, a routed 2nd stage is ERL-capped. Supersedes (cancels) the global head-to-head run 30773422340 via concurrency. 3 seeds parallel A10G. approved (lean, ~$1-2). 2026-08-03T00:15Z
