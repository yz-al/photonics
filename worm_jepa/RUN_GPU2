HEAVY-SOTA affinity run: the error debug (confidence + is-error AUC + clustering) showed the winner's
residual errors are DIFFUSE (CV-AUC ~0.56, silhouette ~0.22), LOW-CONFIDENCE (errors sit closer to the
decision boundary than correct edges), and driven by AFFINITY QUALITY (errors correlate with raw
mean_short/mean_long affinities; LSD/JEPA shape features at chance) -- i.e. no targetable head, the honest
lever is heavier affinity training (we were at ~1k steps vs real SOTA ~400k). This run trains ONLY the sota
affinity net (WORM_S3_SOURCES=sota) at DEC_STEPS=30000 (base) + a warm-started continuation, keeps JEPA(5000)
edge features + LSD, then agglomerates with the baked merge_bias=+1.25 operating point and re-saves the
Volume checkpoint so we can re-run the FREE error debug afterward to confirm the errors actually dropped.
CREMI=Drosophila(fly). 3 seeds parallel A10G, per-seed timeout 5h. approved (bigger SOTA ~30k). 2026-08-04T02:40Z
