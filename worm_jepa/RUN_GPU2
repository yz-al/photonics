mamba long-range run 9a1cafc: test global-context Mamba head against SOTA's long-range failures (79% of its errors). Trains MambaLongRange3D (selective SSM, bidirectional y/x scans) at dense once/seed -> full_preds['mamba']; beat_sota strategies mamba_alone / longrange_from_mamba / ens_sota_mamba, all ERL-gated (a win must not create compounding merges). Also full dense(1,16)+sparse(500,5000) table + edge detector + double black box, healthy encoder (EMA=0.998, VAR=0.6, healthiest-seed analysis), merge-aware ERL. 3 seeds parallel A10G. approved ~$5-7. 2026-08-02T22:20Z
