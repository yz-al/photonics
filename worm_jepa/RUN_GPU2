SOTA+Mamba fusion run 6555c60: does adding a Mamba global-context block into SOTA's U-Net bottleneck beat SOTA? sotamamba (SotaUNet + 2 MambaBlocks at the bottleneck, warm-started from trained SOTA + fine-tuned) vs sota_plus (SOTA + same fine-tune steps, NO mamba) isolates Mamba's contribution at equal budget; vs baseline_sota. Scored on global VOI/Rand/ERL (merge-aware) + conditional net-corrections (fixes vs breaks where the head changed the call, merges/splits separate). dense-16, JEPA 1000, dec 1000 + fine-tune 800, 3 seeds parallel A10G. approved ~$6-8. 2026-08-03T00:55Z
