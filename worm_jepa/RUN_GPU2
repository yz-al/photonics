context-heads head-to-head 86b8795: resolution-fixed Mamba + Transformer + GNN vs SOTA on the long-range errors. All three share one decoder (upsample + raw skip, fixes the coarse-output flaw); trained dense once/seed -> full_preds; beat_sota scores X_alone / longrange_from_X / ens_sota_X for each, ERL-gated (no win via compounding merges). Full dense(1,16)+sparse(500,5000) + edge detector + double black box, healthy encoder (EMA=0.998, VAR=0.6), merge-aware ERL. 3 seeds parallel A10G. approved ~$7-9. 2026-08-02T23:45Z
