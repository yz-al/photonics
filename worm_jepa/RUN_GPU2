fly error-analysis + checkpoint run 2b9f844: the full ladder (MWS vs multicut{affinity} vs multicut{+LSD+JEPA} vs lifted_multicut_full) WITH the diagnostics the user asked for -- VOI split/merge decomposition (VOI_merge=under-seg, VOI_split=over-seg), actual merge/split ERROR COUNTS for MWS vs the winner, and a CREMI-score proxy (geomean VOI x adapted-Rand) to gauge distance from published fly SOTA ~0.22. ALSO saves the agglomeration inputs to the Modal Volume so future error-analysis/tuning is a ~$0.10 CPU rerun (rerun_agglo), not a full retrain. CREMI=Drosophila(fly). Well-trained SOTA + JEPA(5000) + LSD, dense-16, 3 seeds parallel A10G. approved (checkpoint-first). 2026-08-03T18:45Z
