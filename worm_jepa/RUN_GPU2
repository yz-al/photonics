mamba FULL-3D long-range run 09dbb5b: global-context Mamba head with bidirectional z+y+x scans (every voxel sees all three axes both directions). Supersedes the y/x-only run (cancelled early via concurrency). Trains MambaLongRange3D at dense once/seed -> full_preds['mamba']; beat_sota strategies mamba_alone / longrange_from_mamba / ens_sota_mamba, ERL-gated. Full dense(1,16)+sparse(500,5000) + edge detector + double black box, healthy encoder (EMA=0.998, VAR=0.6), merge-aware ERL. 3 seeds parallel A10G. approved ~$5-7. 2026-08-02T22:45Z
